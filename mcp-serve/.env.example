# Base API URL including the /api prefix. The MCP stdio process inherits this
# variable from its launcher; this project does not automatically load .env.
LAUNDRY_API_BASE_URL=http://127.0.0.1:3000/api